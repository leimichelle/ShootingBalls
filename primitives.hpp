#ifndef PRIMITIVES_HPP
#define PRIMITIVES_HPP
#include <stdlib.h>
#include <vector>
void createUnitCube(std::vector<glm::vec3>& vertices, std::vector<glm::vec2>& f_uvs, std::vector<glm::vec3>& normals);

#endif